# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs

__all__ = [
    'GetNodesResult',
    'AwaitableGetNodesResult',
    'get_nodes',
    'get_nodes_output',
]

@pulumi.output_type
class GetNodesResult:
    """
    A collection of values returned by getNodes.
    """
    def __init__(__self__, filter=None, id=None, nodes=None, os=None, prefix=None, resources=None):
        if filter and not isinstance(filter, str):
            raise TypeError("Expected argument 'filter' to be a str")
        pulumi.set(__self__, "filter", filter)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if nodes and not isinstance(nodes, list):
            raise TypeError("Expected argument 'nodes' to be a list")
        pulumi.set(__self__, "nodes", nodes)
        if os and not isinstance(os, bool):
            raise TypeError("Expected argument 'os' to be a bool")
        pulumi.set(__self__, "os", os)
        if prefix and not isinstance(prefix, str):
            raise TypeError("Expected argument 'prefix' to be a str")
        pulumi.set(__self__, "prefix", prefix)
        if resources and not isinstance(resources, bool):
            raise TypeError("Expected argument 'resources' to be a bool")
        pulumi.set(__self__, "resources", resources)

    @_builtins.property
    @pulumi.getter
    def filter(self) -> Optional[_builtins.str]:
        return pulumi.get(self, "filter")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def nodes(self) -> Sequence['outputs.GetNodesNodeResult']:
        """
        `(list of nodes)` - A list of nodes matching the search criteria.
        """
        return pulumi.get(self, "nodes")

    @_builtins.property
    @pulumi.getter
    def os(self) -> Optional[_builtins.bool]:
        return pulumi.get(self, "os")

    @_builtins.property
    @pulumi.getter
    def prefix(self) -> Optional[_builtins.str]:
        return pulumi.get(self, "prefix")

    @_builtins.property
    @pulumi.getter
    def resources(self) -> Optional[_builtins.bool]:
        return pulumi.get(self, "resources")


class AwaitableGetNodesResult(GetNodesResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetNodesResult(
            filter=self.filter,
            id=self.id,
            nodes=self.nodes,
            os=self.os,
            prefix=self.prefix,
            resources=self.resources)


def get_nodes(filter: Optional[_builtins.str] = None,
              os: Optional[_builtins.bool] = None,
              prefix: Optional[_builtins.str] = None,
              resources: Optional[_builtins.bool] = None,
              opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetNodesResult:
    """
    Retrieve a list of nodes from Nomad.

    > **Note:** All node attribute values can change if the node is restarted and
    its fingerprint changes. In particular, the `drain`, `status`, `status_description`,
    and `scheduling_eligibility` fields are ephemeral and can change at any time
    without an agent restart.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_nomad as nomad

    all = nomad.get_nodes()
    ```

    ### Filtering by status

    ```python
    import pulumi
    import pulumi_nomad as nomad

    ready = nomad.get_nodes(filter="Status == \\"ready\\"")
    ```

    ### Including OS attributes and resources

    ```python
    import pulumi
    import pulumi_nomad as nomad

    with_details = nomad.get_nodes(os=True,
        resources=True)
    ```


    :param _builtins.str filter: `(string: <optional>)` - Specifies the [expression][nomad_api_filter]
           used to filter the results.
    :param _builtins.bool os: `(bool: false)` - If true, include special attributes such as operating
           system name in the response. When false, the `attributes` map will not contain
           OS-related attributes.
    :param _builtins.str prefix: `(string: <optional>)` - Specifies a string to filter nodes based on
           an ID prefix. Must have an even number of hexadecimal characters (0-9a-f).
    :param _builtins.bool resources: `(bool: false)` - If true, include `node_resources` and
           `reserved_resources` in the response.
    """
    __args__ = dict()
    __args__['filter'] = filter
    __args__['os'] = os
    __args__['prefix'] = prefix
    __args__['resources'] = resources
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('nomad:index/getNodes:getNodes', __args__, opts=opts, typ=GetNodesResult).value

    return AwaitableGetNodesResult(
        filter=pulumi.get(__ret__, 'filter'),
        id=pulumi.get(__ret__, 'id'),
        nodes=pulumi.get(__ret__, 'nodes'),
        os=pulumi.get(__ret__, 'os'),
        prefix=pulumi.get(__ret__, 'prefix'),
        resources=pulumi.get(__ret__, 'resources'))
def get_nodes_output(filter: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                     os: Optional[pulumi.Input[Optional[_builtins.bool]]] = None,
                     prefix: Optional[pulumi.Input[Optional[_builtins.str]]] = None,
                     resources: Optional[pulumi.Input[Optional[_builtins.bool]]] = None,
                     opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetNodesResult]:
    """
    Retrieve a list of nodes from Nomad.

    > **Note:** All node attribute values can change if the node is restarted and
    its fingerprint changes. In particular, the `drain`, `status`, `status_description`,
    and `scheduling_eligibility` fields are ephemeral and can change at any time
    without an agent restart.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_nomad as nomad

    all = nomad.get_nodes()
    ```

    ### Filtering by status

    ```python
    import pulumi
    import pulumi_nomad as nomad

    ready = nomad.get_nodes(filter="Status == \\"ready\\"")
    ```

    ### Including OS attributes and resources

    ```python
    import pulumi
    import pulumi_nomad as nomad

    with_details = nomad.get_nodes(os=True,
        resources=True)
    ```


    :param _builtins.str filter: `(string: <optional>)` - Specifies the [expression][nomad_api_filter]
           used to filter the results.
    :param _builtins.bool os: `(bool: false)` - If true, include special attributes such as operating
           system name in the response. When false, the `attributes` map will not contain
           OS-related attributes.
    :param _builtins.str prefix: `(string: <optional>)` - Specifies a string to filter nodes based on
           an ID prefix. Must have an even number of hexadecimal characters (0-9a-f).
    :param _builtins.bool resources: `(bool: false)` - If true, include `node_resources` and
           `reserved_resources` in the response.
    """
    __args__ = dict()
    __args__['filter'] = filter
    __args__['os'] = os
    __args__['prefix'] = prefix
    __args__['resources'] = resources
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('nomad:index/getNodes:getNodes', __args__, opts=opts, typ=GetNodesResult)
    return __ret__.apply(lambda __response__: GetNodesResult(
        filter=pulumi.get(__response__, 'filter'),
        id=pulumi.get(__response__, 'id'),
        nodes=pulumi.get(__response__, 'nodes'),
        os=pulumi.get(__response__, 'os'),
        prefix=pulumi.get(__response__, 'prefix'),
        resources=pulumi.get(__response__, 'resources')))
